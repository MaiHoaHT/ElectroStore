
@{
    ViewBag.Title = "View";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@Html.Action("MenuProductCategory", "Menu")
<!-- SECTION -->
@Html.Action("MenuNewProdCate", "Menu")
<!-- /SECTION -->
<!-- HOT DEAL SECTION -->
<div id="hot-deal" class="section">
    <!-- container -->
    <div class="container">
        <!-- row -->
        <div class="row">
            <div class="col-md-12">
                <div class="hot-deal">
                    <ul class="hot-deal-countdown">
                        <li>
                            <div>
                                <h3 id="dayCountdown"></h3>
                                <span>Ngày</span>
                            </div>
                        </li>
                        <li>
                            <div>
                                <h3 id="hourCountdown"></h3>
                                <span>Giờ</span>
                            </div>
                        </li>
                        <li>
                            <div>
                                <h3 id="minsCountdown"></h3>
                                <span>Phút</span>
                            </div>
                        </li>
                        <li>
                            <div>
                                <h3 id="secsCountdown"></h3>
                                <span>Giây</span>
                            </div>
                        </li>
                    </ul>
                    <h2 class="text-uppercase">Deal sốc trong tuần</h2>
                    <p>Bộ sưu tập ưu đãi 50%</p>
                    <a class="primary-btn cta-btn" href="/san-pham">Mua ngay</a>
                </div>
            </div>
        </div>
        <!-- /row -->
    </div>
    <!-- /container -->
</div>
<!-- /HOT DEAL SECTION -->
<!-- SECTION -->
@Html.Action("MenuSaleProdCate", "Menu")
<!-- /SECTION -->
<!-- SECTION -->
<div class="section">
    <!-- container -->
    <div class="container">
        <!-- row -->
        <div class="row">
            <div class="col-md-4 col-xs-6">
                <div class="section-title">
                    <h4 class="title">Sản phẩm bán chạy</h4>
                    <div class="section-nav">
                        <div id="slick-nav-3" class="products-slick-nav"></div>
                    </div>
                </div>

                <div class="products-widget-slick" data-nav="#slick-nav-3">
                    <div>
                        @Html.Action("AllProductItemsSale", "Product", new { top = 0 })
                    </div>
                    <div>
                        @Html.Action("AllProductItemsSale", "Product", new { top = 3 })
                    </div>
                </div>
            </div>

            <div class="col-md-4 col-xs-6">
                <div class="section-title">
                    <h4 class="title">Sản phẩm bán chạy</h4>
                    <div class="section-nav">
                        <div id="slick-nav-4" class="products-slick-nav"></div>
                    </div>
                </div>

                <div class="products-widget-slick" data-nav="#slick-nav-4">
                    <div>
                        @Html.Action("AllProductItemsSale", "Product", new { top = 6 })
                    </div>
                    <div>
                        @Html.Action("AllProductItemsSale", "Product", new { top = 9 })
                    </div>
                </div>
            </div>

            <div class="clearfix visible-sm visible-xs"></div>

            <div class="col-md-4 col-xs-6">
                <div class="section-title">
                    <h4 class="title">Sản phẩm bán chạy</h4>
                    <div class="section-nav">
                        <div id="slick-nav-5" class="products-slick-nav"></div>
                    </div>
                </div>

                <div class="products-widget-slick" data-nav="#slick-nav-5">
                    <div>
                        @Html.Action("AllProductItemsSale", "Product", new { top = 12 })
                    </div>
                    <div>
                        @Html.Action("AllProductItemsSale", "Product", new { top = 15 })
                    </div>
                </div>
            </div>

        </div>
        <!-- /row -->
    </div>
    <!-- /container -->
</div>
<!-- /SECTION -->
<!-- NEWSLETTER -->
<script>
    function countdown() {
        debugger;
        var endDate = new Date("May 31, 2023 00:00:00").getTime(); // Đặt ngày kết thúc đếm ngược ở đây
        var now = new Date().getTime();
        var distance = endDate - now;

        // Tính toán các thành phần của thời gian còn lại
        var days = Math.floor(distance / (1000 * 60 * 60 * 24));
        var hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
        var minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
        var seconds = Math.floor((distance % (1000 * 60)) / 1000);

        // Hiển thị kết quả trong một phần tử có id "countdown"
        document.getElementById("dayCountdown").innerHTML = days;
        document.getElementById("hourCountdown").innerHTML = hours;
        document.getElementById("minsCountdown").innerHTML = minutes;
        document.getElementById("secsCountdown").innerHTML = seconds;

        // Đặt lại hàm này để cập nhật đếm ngược mỗi giây
        setTimeout(countdown, 1000);
    }

    // Gọi hàm countdown để bắt đầu đếm ngược
    countdown();
</script>